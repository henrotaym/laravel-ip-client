{
    "name": "@henrotaym/laravel-ip-client",
    "version": "2.0.0",
    "private": true,
    "license": "MIT",
    "scripts": {
        "push": "git push && git push --tags",
        "postversion": "npm run push"
    }
}
